// import { Page, expect } from '@playwright/test';

// async function navigateAndVerifyTitle(page: Page, link: () => Promise<void | Page>, expectedTitle: string) {
//     const newPage = await link();
//     if (newPage) {
//         await expect(newPage).toHaveTitle(expectedTitle);
//         await newPage.close();
//     }
// }
